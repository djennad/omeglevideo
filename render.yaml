services:
  - type: web
    name: omeglevideo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gevent wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: PORT
        value: 10000
